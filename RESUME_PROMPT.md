# RFP Demo — 작업 재개 프롬프트

> 이어지는 세션에서 작업을 재개할 때 아래 블록을 복사해 에이전트에게 전달하세요.

---

## 재개용 프롬프트 (복사해서 사용)

```
RFP Demo 프로젝트 작업을 이어서 진행합니다. 먼저 아래를 순서대로 수행해
현재 상태를 파악한 뒤, 나에게 다음 작업 방향을 제안해줘.

1. AGENTS.md 를 읽고 작업 규칙(절대 규칙 15개 + 문서 관리 규칙 7개)을 숙지한다.
2. 현재 브랜치와 작업 디렉터리 상태를 확인한다.
3. docs/agent/rfp-pipeline-spec.md 의 §16과 §17을 읽어 ✅/⚠️/🔴 상태를 파악한다.
   이 파일이 단일 진실 공급원이다.
4. 완료된 작업과 남은 작업을 표로 정리하고, 다음 진행 후보를
   우선순위/노력/사이드이펙트 관점에서 3~4개 제안한 뒤, 어느 것부터 할지 물어봐.

남은 작업 후보(참고용):
- P2: 17.22 진행률 세분화, 17.23 ID coverage 검증, 17.24 DB 백업 전략
- 차단됨: 17.19 벡터 검색 (EMBEDDING_API_URL 미설정)

작업 규칙:
- 한 세션에 단계 하나 원칙, 사이드이펙트 없는 작업은 동의 하에 묶어 커밋 가능.
- 코드 수정 전 실제 코드 확인 (문서 가정 금지).
- 매 단계 npx tsc --noEmit 으로 검증 (운영 코드 오류 0건 유지, scripts/ 8건은 기존).
- 커밋/push는 내 명시적 지시가 있을 때만.
- 단계 완료 후 spec §16/§17 상태 갱신.
- 새 브랜치는 필요 시 내가 지시.
```

---

## 항목 지정 한 줄 (위 프롬프트 끝에 추가)

```
17.22 진행률 세분화 진행해줘.
17.23 ID coverage 검증 구현해줘.
새 브랜치 만들고 진행.
작업 끝나면 커밋 + push까지.
```

---

## 현재 진행 상황 (2026-07-14 기준)

### 전체 완료
```
P0  ████████████ 7/7 ✅
P1  ████████████ 5/5 ✅
P2  ██████████░░ 5/8   (신규 P2 3건 🔴, 인프라 블록 17.19 🔴)
⚠️  1건 (16.1 추출 커버리지 — LLM 모델 의존)
```

### 브랜치 트리
```
feat/similar-ui-ux (UI/UX + 문서정리)
  └─ fix/p0-hardening (P0 하드닝 4건)
      └─ feat/ux-improvements (UX 3건)
          └─ feat/llm-description-extraction (LLM 설명 파이프라인)
              └─ fix/p1-code-quality (P1 5건 + P0 재분석)
                  └─ feat/p2-matrix-filter (P2 5건) ← HEAD
```

전체 6개 브랜치 모두 push 완료.

### 핵심 구현 사항
- **description 파이프라인**: 영문 name-only 프롬프트 + `cleanDescription` 6단계 (보일러플레이트 제거 + `reflowLines`)
- **프론트**: 매트릭스 정렬/필터/불릿, 유사검색 CTA, 지표 툴팁, a11y
- **인프라**: FK CASCADE(8개), GIN 인덱스(2개), Worker Pool 다중 Job 동시 처리
- **코드 품질**: dead code 11건(-1,613행), 타입 any 정리, env 단일화

### 남은 작업 (P2, 모두 🔴)
| §17 | 내용 |
|---|---|
| 17.22 | 분석 진행률 구간 세분화 (N/59 카운터) |
| 17.23 | ID 기반 coverage 검증 (<0.85 시 고품질 모델 재처리) |
| 17.24 | DB 백업 전략 문서화 (pg_dump + 복구 테스트) |
| 17.19 | 벡터 검색 (EMBEDDING_API_URL 설정 시) |

### 환경
- 패키지: `pnpm`
- LLM: `minimax-m2.7` (영문 name-only 프롬프트, 분석 21~61초)
- 검증: `npx tsc --noEmit` (운영 0건, scripts/ 8건 기존)
- DB: PostgreSQL + pgvector (로컬 Docker, port 5432)
- 단일 진실 공급원: `docs/agent/rfp-pipeline-spec.md` (1,636행)
