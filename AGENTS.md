RFP Demo 에이전트 작업 규칙

목표

기존 RFP Demo 프로젝트의 동작을 보존하면서 요구사항 추출 파이프라인을 점진적으로 개선한다.

상세 요구사항과 전체 로드맵은 docs/agent/rfp-pipeline-spec.md에 있다. 현재 지시된 단계와 관련된 부분만 읽고 작업한다.

절대 규칙

1. 실제 저장소와 현재 브랜치의 코드를 먼저 확인한다.
2. 문서에 적힌 파일이나 구조가 실제로 존재한다고 가정하지 않는다.
3. 한 세션에서는 사용자가 지정한 단계 하나만 수행한다.
4. 현재 단계가 끝나면 결과를 보고하고 중단한다.
5. 사용자의 명시적 지시 없이 다음 단계로 진행하지 않는다.
6. 기존 기능을 전면 재작성하거나 불필요하게 아키텍처를 교체하지 않는다.
7. 기존 API 필드를 제거하지 않는다. 필요한 경우 새 필드만 추가한다.
8. 실제 API 키, DATABASE_URL, 비밀번호 또는 토큰 값을 출력하지 않는다.
9. 비밀정보를 발견하면 값은 가리고 파일 경로와 종류만 보고한다.
10. 사용자의 명시적 지시 없이 다음 명령을 실행하지 않는다.

* git commit
* git push
* git reset
* git clean
* rebase
* 운영 DB 마이그레이션 적용
* TRUNCATE, DROP 또는 대량 DELETE

11. 마이그레이션 파일은 검토할 수 있지만 자동으로 운영 DB에 적용하지 않는다.
12. 존재하지 않는 테스트 명령, 테스트 데이터 또는 구현을 실행·검증했다고 보고하지 않는다.
13. TypeScript 오류를 불필요한 any로 숨기지 않는다.
14. 테스트 결과와 성능 수치를 추정하거나 만들어내지 않는다.
15. 테스트 입력이 없으면 해당 항목을 검증 불가로 기록한다.

단계별 작업 순서

각 단계는 다음 순서로 진행한다.

1. 현재 구현 조사
2. 실제 원인과 영향 범위 확인
3. 최소 수정안 작성
4. 지정된 범위만 수정
5. 실제 package.json에 존재하는 검증 명령 실행
6. git diff 검토
7. 결과 보고
8. 중단

변경 범위

사용자가 지정한 단계와 직접 관련되지 않은 파일은 수정하지 않는다.

예상보다 넓은 변경이 필요하면 코드를 수정하지 말고 다음 내용을 먼저 보고한다.

* 필요한 추가 변경
* 변경 대상 파일
* 기존 기능에 대한 위험
* 더 작은 대안

검증

검증 명령은 package.json에서 실제 스크립트를 확인한 뒤 실행한다.

실패한 검증을 숨기지 않는다. 환경 문제, 기존 실패, 이번 변경으로 인한 실패를 구분한다.

단계 완료 보고

각 단계가 끝나면 다음 형식으로 보고한다.

1. 확인한 실제 원인
2. 수정한 파일
3. 변경한 동작
4. 기존 기능에 미치는 영향
5. 실행한 명령
6. 테스트 결과
7. 검증하지 못한 항목
8. 남은 위험
9. 권장 다음 단계

커밋은 수행하지 않는다. 대신 권장 커밋 메시지만 제안한다.